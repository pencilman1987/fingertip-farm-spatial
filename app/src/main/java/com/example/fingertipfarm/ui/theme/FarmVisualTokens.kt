package com.example.fingertipfarm.ui.theme

import androidx.compose.ui.graphics.Color
import com.pico.spatial.ui.foundation.vibrant.Vibrant
import com.pico.spatial.ui.foundation.vibrant.withVibrant

object FarmColors {
    val Soil = Color(0xFF8D5524).withVibrant(Vibrant.None) // design-style: fixed-figma-color original CSS --soil
    val Grass = Color(0xFF4ADE80).withVibrant(Vibrant.None) // design-style: fixed-figma-color original CSS --grass
    val GrassDark = Color(0xFF2F7D32).withVibrant(Vibrant.None) // design-style: fixed-figma-color original CSS planted-key depth
    val Water = Color(0xFF38BDF8).withVibrant(Vibrant.None) // design-style: fixed-figma-color original CSS --water-color
    val WaterDark = Color(0xFF0284C7).withVibrant(Vibrant.None) // design-style: fixed-figma-color original CSS --water-dark
    val WoodDark = Color(0xFF5D4037).withVibrant(Vibrant.None) // design-style: fixed-figma-color original CSS --wood-dark
    val WoodLight = Color(0xFF8D6E63).withVibrant(Vibrant.None) // design-style: fixed-figma-color original CSS --wood-light
    val Cream = Color(0xFFFFFEF0).withVibrant(Vibrant.None) // design-style: fixed-figma-color original CSS modal surface
    val Gold = Color(0xFFF9C74F).withVibrant(Vibrant.None) // design-style: fixed-figma-color sampled original logo highlight
    val ModalBorder = Color(0xFFD7CCC8).withVibrant(Vibrant.None) // design-style: fixed-figma-color original CSS modal border
    val SceneScrim = Color(0x66000000).withVibrant(Vibrant.None) // design-style: fixed-figma-color original CSS modal overlay rgba(0,0,0,.4)
    val CrtFrame = Color(0xFF1A1A1A).withVibrant(Vibrant.None) // design-style: fixed-figma-color original CSS CRT frame
    val CrtScreen = Color(0xFF08120A).withVibrant(Vibrant.None) // design-style: fixed-figma-color original CSS CRT screen
    val CrtGreen = Color(0xFF33FF33).withVibrant(Vibrant.None) // design-style: fixed-figma-color original CSS CRT phosphor
}
